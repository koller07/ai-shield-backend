# AI-Shield Backend

Backend for AI-Shield Chrome Extension

## Setup

1. Create `.env` file with DATABASE_URL
2. Run `npm install`
3. Run `npm start`

## API Endpoints

- POST /api/detection - Record a detection
- GET /api/report/:companyId/:month/:year - Get report
- GET /api/health - Health check
